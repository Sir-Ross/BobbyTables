const {prefix,token} = require('../../config.json');

module.exports = {
	name: 			'',
	description: 	'',
	active: 		true,
	usage: 			`\`${prefix}\``,
	async execute(client,message,args){
		
	}
}